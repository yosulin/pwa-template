<!-- PLACEHOLDER — sustituye por el icono real del proyecto.
     Este es válido como favicon/apple-touch-icon (puede llegar al borde). -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <rect width="64" height="64" rx="14" fill="{{THEME_COLOR}}" />
  <circle cx="32" cy="32" r="18" fill="{{ACCENT}}" />
</svg>
