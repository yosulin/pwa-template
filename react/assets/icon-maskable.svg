<!-- PLACEHOLDER — variante "maskable": fondo a sangre completa (sin radio),
     contenido importante dentro del 80% central (zona segura de Android). -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <rect width="64" height="64" fill="{{THEME_COLOR}}" />
  <circle cx="32" cy="32" r="16" fill="{{ACCENT}}" />
</svg>
